// Which push this is.
//
// Shown at the bottom of every screen and used as the title of the apk
// release, so the number on the phone and the number on the releases page are
// the same number. That is the whole job: open the app, read the bottom line,
// know whether you are on the build you just asked for.
//
// BUMP THIS ON EVERY PUSH. It is one line and nothing else keeps it honest.
export const VERSION = 'gailey.v.02';
